rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /ReadyPlayerOne/{document} {
      allow read, write: if true; // For development - make more restrictive in production
    }
  }
}

